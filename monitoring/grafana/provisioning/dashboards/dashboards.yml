apiVersion: 1

providers:
  - name: "Flight Delay Prediction App"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: "/etc/grafana/provisioning/dashboards"
