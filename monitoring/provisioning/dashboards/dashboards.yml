apiVersion: 1

providers:
  - name: "Digit Visual Recognition Dashboards"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: "/etc/grafana/provisioning/dashboards"
